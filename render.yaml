services:
  - type: web
    name: kokoro-tts
    env: docker
    plan: starter
    autoDeploy: false
    region: oregon
    dockerfilePath: docker/cpu/Dockerfile
    healthCheckPath: /docs
    envVars:
      - key: PORT
        value: 8880